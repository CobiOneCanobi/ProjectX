source 'https://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.5.1'
# Use sqlite3 as the database for Active Record
gem 'sqlite3'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# See https://github.com/rails/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use Unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'
end

group :development do
  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'
end

group :production do
  gem 'pg'
  gem 'rails_12factor'
end


gem 'devise' # Flexible authentication solution for Rails with Warden
gem 'cancancan' # CanCan is an authorization library for Ruby on Rails which restricts what resources a given user is allowed to access.
gem "paranoia", "~> 2.0" # ActiveRecord plugin allowing you to hide and restore records without actually deleting them.
gem 'validates' # Collection of useful custom validators for Rails applications
gem 'acts-as-taggable-on', '~> 3.4' # A tagging plugin for Rails applications that allows for custom tagging along dynamic contexts.
gem 'kaminari' #  A Scope & Engine based, clean, powerful, customizable and sophisticated paginator.
gem 'slack-notifier' # A simple wrapper for posting to slack channels
gem "letter_opener", :group => :development # Preview mail in the browser instead of sending.
gem 'carrierwave' # Carrierwave is a classier solution for file uploads for Rails, Sinatra and other Ruby web frameworks.
gem 'mini_magick' #  MiniMagick is a ruby wrapper for ImageMagick or GraphicsMagick command line.
gem 'fog' # Fog is the Ruby cloud services library, top to bottom.
gem 'ransack' # Ransack enables the creation of both simple and advanced search forms for your Ruby on Rails application.
gem 'whenever', :require => false # Cron jobs in Ruby
gem 'sidekiq' # Simple, efficient background processing for Ruby
gem 'sidekiq-failures', '~> 0.4' # Keep logs of failed sidekiq jobs
gem 'formtastic', '~> 3.0' # A Rails form builder plugin with semantically rich and accessible markup.
gem "cocoon" # Dynamic nested forms using jQuery made easy
gem "figaro" # Simple Rails app configuration
gem 'activeadmin', github: 'activeadmin' # The administration framework for Ruby on Rails applications.
gem 'pry-rails', :group => :development # Easy to read rails console
gem "better_errors", :group => :development # Better error page for Rack apps
gem 'rubocop', require: false # A Ruby static code analyzer, based on the community Ruby style guide.
gem 'capybara' # Capybara helps you test web applications by simulating how a real user would interact with your app.
gem 'factory_girl_rails' # factory_girl is a fixtures replacement
gem 'quiet_assets', group: :development # Mutes assets pipeline log messages.
gem 'haml-rails', '~> 0.9' # Used in almost all views
gem "faker", group: :development # A library for generating fake data such as names, addresses, and phone numbers.
gem 'geocoder' # Complete Ruby geocoding solution
gem 'auto_strip_attributes',       '~> 2.0'    # Auto-strips form inputs
gem 'chronic' # Chronic is a pure Ruby natural language date parser.
gem 'draper', '~> 2.1' # Decorator support
gem 'wicked' # Wizard builder
gem 'gmaps4rails' # Enables easy Google map + overlays creation
gem 'bourbon' # A simple and lightweight mixin library for Sass
gem 'neat' # Connected to bourbon
gem 'bitters' # Connected to bourbon
gem "refills", group: :development # Conntected
  # s.add_dependency 'valid_email2',                '~> 1.2'    # Email Validation
  # s.add_dependency 'validate_url',                '~> 1.0'    # URL Validation
  # s.add_dependency 'validates_timeliness',        '~> 4.0'    # Time validation
  # s.add_dependency 'phony_rails',                 '~> 0.12'   # Phone number validation
  # s.add_dependency 'validates_zipcode',           '~> 0.0'    # Postal code validation